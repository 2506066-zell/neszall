{
  "name": "cute-futura",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel deploy --prebuilt",
    "deploy:prod": "vercel deploy --prebuilt --prod",
    "audit:css": "node scripts/css-audit.js",
    "check:css": "node scripts/css-fetch-check.js http://localhost:3000",
    "serve:ci": "http-server -p 3000 public",
    "test": "npm run audit:css && npm run serve:ci & npm run check:css",
    "test:api": "cross-env NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "cross-env": "^7.0.3",
    "express": "^5.2.1",
    "dotenv": "^17.2.4",
    "vercel": "^33.7.0"
  },
  "engines": {
    "node": "20.x"
  }
}
